# SQLite version 3.x
#   gem install sqlite3
development:
    host: localhost
    adapter: postgresql
    encoding: unicode
    database: tramp_development
    pool: 5
    username: derek
    password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    host: localhost
    adapter: postgresql
    encoding: unicode
    database: tramp_test
    pool: 5
    username: derek
    password:

production:
    host: localhost
    adapter: postgresql
    encoding: unicode
    database: tramp_production
    pool: 5
    username: derek
    password:
